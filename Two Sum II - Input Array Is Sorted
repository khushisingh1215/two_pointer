class Solution {
public:
    vector<int> twoSum(vector<int>& numbers, int target) {
        unordered_map<int,int>m;
      int  n=numbers.size();
        vector<int>ans;
        int sum=0;
        for(int i=0;i<n;i++){
            sum=sum+numbers[i];
            
            if(sum==target){

            }
        }
    }
};
